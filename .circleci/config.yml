version: 2.1

orbs:
  aws-s3: circleci/aws-s3@3.0

jobs:
  test:
    docker:
      - image: cimg/node:14.17
    steps:
      - checkout
      - run: yarn install
      - run: yarn jest
  deploy:
    steps:
      - checkout
      - run: yarn run build
      - aws-s3/copy:
          from: dist
          to: 's3://circleci_tester'
          overwrite: true

workflows:
  main:
    jobs:
      - test
      - deploy:
          filters:
            branches:
              only: main
